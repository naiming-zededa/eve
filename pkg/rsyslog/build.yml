image: eve-rsyslog
org: lfedge
network: yes
config:
  binds:
    - /run:/run
    - /dev:/dev
    - /etc/resolv.conf:/etc/resolv.conf
    - /persist:/persist
    - /config:/config
  net: host
  capabilities:
    - all
  pid: host
