image: edgeview
org: lfedge
network: yes
config:
  binds:
    - /:/hostfs:ro
    - /dev:/dev:ro
    - /run:/run:ro
    - /persist:/persist:ro
    - /config:/config:ro
    - /etc/resolv.conf:/etc/resolv.conf:ro
    - /proc:/host/proc:ro
  capabilities:
    - all
  net: host
  pid: host